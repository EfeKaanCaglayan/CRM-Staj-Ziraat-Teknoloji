<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:ZTCRM.ViewModels"
        x:Class="ZTCRM.UI.Views.CustomerView"
        x:DataType="vm:CustomerViewModel"
        Title="ZTCRM — Müşteri Paneli"
        WindowState="Maximized"
        CanResize="True"
        WindowStartupLocation="CenterScreen"
        xmlns:models="using:ZTCRM.Models"
        xmlns:dataAnnotations="clr-namespace:System.ComponentModel.DataAnnotations;assembly=System.ComponentModel.Annotations"
        Background="#2C2C2C"
        Icon="/Assets/Logo2.png">

    <DockPanel>

        <!-- HEADER -->
        <Border DockPanel.Dock="Top" Background="#2C2C2C" Height="68" ZIndex="10">
            <Grid ColumnDefinitions="Auto,*,Auto" Margin="24,0">
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="12"
                            VerticalAlignment="Center">
                    <Image Source="/Assets/Logo2.png" Width="52" Height="52"/>
                    <StackPanel VerticalAlignment="Center" Spacing="1">
                        <TextBlock Text="ZTCRM" Foreground="White" FontSize="18"
                                   FontWeight="Medium" LetterSpacing="3"/>
                        <TextBlock Text="ŞİKAYET YÖNETİM SİSTEMİ"
                                   Foreground="#AAAAAA" FontSize="9" LetterSpacing="2"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="2" Orientation="Horizontal"
                            Spacing="12" VerticalAlignment="Center">
                    <TextBlock Text="{Binding WelcomeMessage}"
                               Foreground="#AAAAAA" FontSize="12"/>
                    <TextBlock Text="|" Foreground="#3A3A3A" FontSize="11"/>
                    <TextBlock Text="v1.0.0" Foreground="#444444" FontSize="11"/>
                    <TextBlock Text="|" Foreground="#3A3A3A" FontSize="11"/>
                    <Button Click="NotificationButton_Click"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="4"
                            VerticalAlignment="Center">
                        <Grid>
                            <PathIcon Width="18" Height="18" Foreground="#AAAAAA"
                                      Data="M12 22c1.1 0 2-.9 2-2h-4c0 1.1.9 2 2 2zm6-6v-5c0-3.07-1.64-5.64-4.5-6.32V4c0-.83-.67-1.5-1.5-1.5s-1.5.67-1.5 1.5v.68C7.63 5.36 6 7.92 6 11v5l-2 2v1h16v-1l-2-2z"/>
                            <Border Background="#E8182E" CornerRadius="8"
                                    HorizontalAlignment="Right" VerticalAlignment="Top"
                                    Padding="3,1"
                                    IsVisible="{Binding HasUnread}">
                                <TextBlock Text="{Binding UnreadCount}"
                                           FontSize="9" Foreground="White" FontWeight="Bold"/>
                            </Border>
                        </Grid>
                    </Button>
                    <Button Content="Çıkış Yap"
                            Click="LogoutButton_Click"
                            Background="Transparent"
                            Foreground="#AAAAAA"
                            FontSize="12"
                            Padding="8,4"
                            CornerRadius="6"
                            VerticalAlignment="Center"
                            BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Kırmızı çizgi -->
        <Border DockPanel.Dock="Top" Height="3" Background="#E8182E"/>
        <Border DockPanel.Dock="Top" Height="1" Background="#3A3A3A"/>

        <!-- FOOTER -->
        <Border DockPanel.Dock="Bottom" Background="#2C2C2C" Height="44"
                BorderThickness="0,1,0,0" BorderBrush="#3A3A3A">
            <Grid ColumnDefinitions="Auto,*,Auto" Margin="24,0">
                <TextBlock Grid.Column="0" Text="Güvenli bağlantı aktif"
                           Foreground="#444444" FontSize="11" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="© 2026 ZTCRM — Şikayet Yönetim Sistemi"
                           Foreground="#333333" FontSize="11" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- ORTA -->
        <Grid>
            <Border>
                <Border.Background>
                    <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,100%">
                        <GradientStop Color="#C8102E" Offset="0"/>
                        <GradientStop Color="#8C0B1F" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
            </Border>

            <Canvas>
                <Ellipse Canvas.Left="600" Canvas.Top="-140"
                         Width="560" Height="560"
                         Fill="#A50E26" Opacity="0.5"/>
                <Ellipse Canvas.Left="-160" Canvas.Top="300"
                         Width="480" Height="480"
                         Fill="#A50E26" Opacity="0.4"/>
            </Canvas>

            <Grid ColumnDefinitions="360,*" Margin="32">

                <!-- SOL: Yeni Şikayet Formu -->
                <Border Grid.Column="0" Background="White" CornerRadius="12"
                        Padding="28,24" Margin="0,0,16,0" VerticalAlignment="Top">
                    <Border.BoxShadow>0 8 40 0 #40000000</Border.BoxShadow>
                    <StackPanel Spacing="0">

                        <Border BorderThickness="0,0,0,2" BorderBrush="#E8182E"
                                Padding="0,0,0,16" Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal" Spacing="12">
                                <Border Width="36" Height="36" CornerRadius="8" Background="#E8182E">
                                    <PathIcon Width="16" Height="16" Foreground="White"
                                              Data="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-7 3c1.93 0 3.5 1.57 3.5 3.5S13.93 13 12 13s-3.5-1.57-3.5-3.5S10.07 6 12 6zm7 13H5v-.23c0-.62.28-1.2.76-1.58C7.47 15.82 9.64 15 12 15s4.53.82 6.24 2.19c.48.38.76.97.76 1.58V19z"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center" Spacing="2">
                                    <TextBlock Text="Yeni Şikayet"
                                               FontSize="14" FontWeight="Medium"
                                               Foreground="#111111"/>
                                    <TextBlock Text="Talebinizi iletin"
                                               FontSize="11" Foreground="#999999"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <TextBlock Text="Şikayet Türü" FontSize="12"
                                   Foreground="#555555" Margin="0,0,0,6"/>
                        <ComboBox ItemsSource="{Binding RequestTypes}"
                                  SelectedItem="{Binding RequestType}"
                                  PlaceholderText="Şikayet türü seçin"
                                  HorizontalAlignment="Stretch"
                                  Height="38" FontSize="13"
                                  Margin="0,0,0,14"/>

                        <TextBlock Text="Açıklama" FontSize="12"
                                   Foreground="#555555" Margin="0,0,0,6"/>
                        <TextBox PlaceholderText="Şikayetinizi detaylı açıklayın..."
                                 Text="{Binding Description}"
                                 Height="100" FontSize="13"
                                 Background="#F8F8F8" Foreground="#111111"
                                 BorderBrush="#DDDDDD" BorderThickness="1"
                                 CornerRadius="6" Padding="10,8"
                                 AcceptsReturn="True" TextWrapping="Wrap"
                                 Margin="0,0,0,16"/>

                        <Border Background="#FEF2F2" BorderBrush="#FECACA"
                                BorderThickness="1" CornerRadius="8"
                                Padding="12,10" Margin="0,0,0,12"
                                IsVisible="{Binding ErrorMessage,
                                            Converter={x:Static StringConverters.IsNotNullOrEmpty}}">
                            <TextBlock Text="{Binding ErrorMessage}"
                                       Foreground="#B91C1C" FontSize="12"
                                       TextWrapping="Wrap"/>
                        </Border>

                        <Border Background="#F0FDF4" BorderBrush="#BBF7D0"
                                BorderThickness="1" CornerRadius="8"
                                Padding="12,10" Margin="0,0,0,12"
                                IsVisible="{Binding SuccessMessage,
                                            Converter={x:Static StringConverters.IsNotNullOrEmpty}}">
                            <TextBlock Text="{Binding SuccessMessage}"
                                       Foreground="#15803D" FontSize="12"
                                       TextWrapping="Wrap"/>
                        </Border>

                        <Button Content="Onay Al"
                                Command="{Binding GetApprovalCommand}"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Center"
                                Height="42" FontSize="13" FontWeight="Medium"
                                Background="#555555" Foreground="White"
                                CornerRadius="8" Margin="0,0,0,8"/>

                        <Border Background="#F0F9FF" BorderBrush="#BAE6FD"
                                BorderThickness="1" CornerRadius="8"
                                Padding="12,10" Margin="0,0,0,12"
                                IsVisible="{Binding AiMessage,
                                            Converter={x:Static StringConverters.IsNotNullOrEmpty}}">
                            <TextBlock Text="{Binding AiMessage}"
                                       Foreground="#0369A1" FontSize="12"
                                       TextWrapping="Wrap"/>
                        </Border>

                        <Button Content="Şikayet Gönder"
                                Command="{Binding CreateRequestCommand}"
                                IsEnabled="{Binding IsApproved}"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Center"
                                Height="42" FontSize="13" FontWeight="Medium"
                                Background="#E8182E" Foreground="White"
                                CornerRadius="8"/>
                    </StackPanel>
                </Border>

                <!-- SAĞ: Şikayet Listesi -->
                <Border Grid.Column="1" Background="White" CornerRadius="12"
                        Padding="28,24">
                    <Border.BoxShadow>0 8 40 0 #40000000</Border.BoxShadow>
                    <DockPanel>

                        <Border DockPanel.Dock="Top"
                                BorderThickness="0,0,0,2" BorderBrush="#E8182E"
                                Padding="0,0,0,16" Margin="0,0,0,20">
                            <Grid ColumnDefinitions="*,Auto">
                                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="12">
                                    <Border Width="36" Height="36" CornerRadius="8" Background="#E8182E">
                                        <PathIcon Width="16" Height="16" Foreground="White"
                                                  Data="M3 13h2v-2H3v2zm0 4h2v-2H3v2zm0-8h2V7H3v2zm4 4h14v-2H7v2zm0 4h14v-2H7v2zM7 7v2h14V7H7z"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center" Spacing="2">
                                        <TextBlock Text="Şikayetlerim"
                                                   FontSize="14" FontWeight="Medium"
                                                   Foreground="#111111"/>
                                        <TextBlock Text="Tüm talepleriniz"
                                                   FontSize="11" Foreground="#999999"/>
                                    </StackPanel>
                                </StackPanel>
                                <ToggleButton Grid.Column="1"
                                              IsChecked="{Binding ShowClosed, Mode=TwoWay}"
                                              VerticalAlignment="Center"
                                              Padding="12,6"
                                              CornerRadius="6"
                                              FontSize="12">
                                    <ToggleButton.Styles>
                                        <Style Selector="ToggleButton">
                                            <Setter Property="Background" Value="#F5F5F5"/>
                                            <Setter Property="Foreground" Value="#555555"/>
                                            <Setter Property="Content" Value="Kapalıları Göster"/>
                                        </Style>
                                        <Style Selector="ToggleButton:checked">
                                            <Setter Property="Background" Value="#E8182E"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="Content" Value="Kapalıları Gizle"/>
                                        </Style>
                                    </ToggleButton.Styles>
                                </ToggleButton>
                            </Grid>
                        </Border>

                        <DataGrid Sorting="RequestsGrid_Sorting"
                                  ItemsSource="{Binding FilteredRequests}"
                                  SelectedItem="{Binding SelectedRequest}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  CanUserSortColumns="True"
                                  CanUserReorderColumns="False"
                                  GridLinesVisibility="Horizontal"
                                  BorderThickness="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="No"
                                                    Binding="{Binding RequestId}"
                                                    Width="55"
                                                    CanUserSort="True"/>
                                <DataGridTextColumn Header="Tür"
                                                    Binding="{Binding RequestType}"
                                                    Width="130"
                                                    CanUserSort="True"/>
                                <DataGridTextColumn Header="Açıklama"
                                                    Binding="{Binding Description}"
                                                    Width="*"/>
                                <DataGridTextColumn Header="Durum"
                                                    Binding="{Binding CurrentStatus}"
                                                    Width="90"
                                                    CanUserSort="True"/>
                                <DataGridTextColumn Header="Tarih"
                                                    Binding="{Binding CreatedAt, StringFormat='{}{0:dd.MM.yyyy}'}"
                                                    Width="90"
                                                    CanUserSort="True"/>
                                <DataGridTemplateColumn Header="Detay" Width="85">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate x:DataType="models:ServiceRequest">
                                            <Button Content="Detay"
                                                    Tag="{Binding}"
                                                    Click="DetailButton_Click"
                                                    HorizontalAlignment="Center"
                                                    Height="26" FontSize="11"
                                                    Background="#E8182E" Foreground="White"
                                                    CornerRadius="4" Padding="10,0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="İptal" Width="70">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate x:DataType="models:ServiceRequest">
                                            <Button Content="İptal"
                                                    Tag="{Binding}"
                                                    Click="CancelButton_Click"
                                                    HorizontalAlignment="Center"
                                                    Height="26" FontSize="11"
                                                    Background="#555555" Foreground="White"
                                                    CornerRadius="4" Padding="10,0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                    </DockPanel>
                </Border>

            </Grid>
        </Grid>

    </DockPanel>
</Window>